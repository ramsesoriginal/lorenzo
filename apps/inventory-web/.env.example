# Copy to .env and fill in. See docs/operations/local-authgear-setup.md for
# where these come from - this app needs its own Authgear "Single Page
# Application" client (a public/PKCE client, distinct from apps/api's own
# dev-token-script client), with its Authorized Redirect URIs set to
# http://localhost:4321/auth/redirect for local dev.

PUBLIC_AUTHGEAR_ENDPOINT=
PUBLIC_AUTHGEAR_CLIENT_ID=

# Optional - defaults to the deployed Cloud Run API. Set this to point at a
# local apps/api instance instead (e.g. http://localhost:8000).
PUBLIC_API_BASE_URL=
