version: 2

updates:
  # Add a package-ecosystem entry per app/package once it exists — see
  # docs/guides/adding-an-app.md. Only github-actions applies right now.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore(ci)"
