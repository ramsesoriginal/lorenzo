# Architecture Decision Records

Numbered sequentially, never renumbered. Superseded ADRs stay in place with a status update pointing at the replacement — see [0009](0009-identity-provider-authgear.md). Numbers have already collided many times across long-lived parallel branches (see the renumbering and collision-avoidance notes on 0050, 0054, 0068, 0069, 0070, 0075, 0077, 0087, and 0096 below) — to shrink that window, put a new RFC/ADR on its own small branch and merge the document itself as soon as the decision is settled, before the rest of the implementation lands; see [ADR 0070](0070-planning-milestones-issues-and-a-deferred-roadmap.md).

- [0000 - Template](0000-template.md)
- [0001 - Record architecture decisions as ADRs](0001-record-architecture-decisions.md)
- [0002 - Multi-tenancy: shared schema + PostgreSQL row-level security](0002-multi-tenancy-shared-schema-rls.md)
- [0003 - Polyglot monorepo tooling](0003-polyglot-monorepo-tooling.md)
- [0004 - Web frontends are static Astro sites](0004-static-astro-frontend.md)
- [0005 - Git branching and merge strategy](0005-git-branching-and-merge-strategy.md)
- [0006 - License: AGPL-3.0](0006-agpl-3.0-license.md)
- [0007 - apps/ layout: one directory per deployable app, multiplicity per type](0007-apps-layout-and-multiplicity.md)
- [0008 - Background jobs and rate limiting: taskiq + fastapi-limiter, deferred](0008-deferred-taskiq-and-fastapi-limiter.md)
- [0009 - Identity provider: Authgear (self-hosted OIDC)](0009-identity-provider-authgear.md)
- [0010 - User, tenant, and membership model](0010-user-tenant-membership-model.md)
- [0011 - Deploy target: Google Cloud Run + Neon](0011-deploy-target-cloud-run-neon.md)
- [0012 - The entity table](0012-entity-table.md)
- [0013 - Tenant table bootstrap](0013-tenant-table-bootstrap.md)
- [0014 - Stats: definitions, groups, and per-entity values](0014-stats.md)
- [0015 - entity_prototype: the inheritance graph](0015-entity-prototype.md)
- [0016 - Containment: the generic entity-to-entity physical relation](0016-containment.md)
- [0017 - Information and payloads](0017-information-and-payloads.md)
- [0018 - SQLAlchemy modeling conventions: type map, relationships, cascades](0018-sqlalchemy-modeling-conventions.md)
- [0019 - Item, item instances, and the v_item/v_item_instance views](0019-item-and-v-item.md)
- [0020 - REST API: tenant path-scoping, schema, and pagination conventions](0020-rest-api-tenant-scoping-and-schemas.md)
- [0021 - A restricted app role, so RLS actually enforces something](0021-restricted-app-role-for-rls-enforcement.md)
- [0022 - User, full Tenant, and Membership](0022-user-tenant-membership.md)
- [0023 - Authgear token verification](0023-authgear-token-verification.md)
- [0024 - Campaign and Player](0024-campaign-and-player.md)
- [0025 - Character (being), roster reuse, and ownership](0025-character-being-and-ownership.md)
- [0026 - Campaign GM, orga, and the campaign access rule](0026-campaign-gm-orga-and-access-rule.md)
- [0027 - Authgear Cloud, not self-hosted](0027-authgear-cloud-not-self-hosted.md)
- [0028 - Knowledge, group membership, and public information](0028-knowledge-and-group-membership.md)
- [0029 - Attribution: created_by/updated_by](0029-attribution-created-by-updated-by.md)
- [0030 - Tenant and campaign read REST API](0030-tenant-campaign-read-api.md)
- [0031 - Character table, and the user/membership/player/GM read REST API](0031-character-table-and-read-api.md)
- [0032 - Item and item-instance CRUD API](0032-item-and-item-instance-crud-api.md)
- [0033 - Tenant creation and update API](0033-tenant-creation-and-update-api.md)
- [0034 - Campaign CRUD API](0034-campaign-crud-api.md)
- [0035 - Campaign-scoped GM visibility](0035-campaign-scoped-gm-visibility.md)
- [0036 - User, player, and character CRUD API](0036-user-player-character-crud-api.md)
- [0037 - Effective stat resolution over the prototype graph](0037-effective-stat-resolution.md)
- [0038 - Information, payload, and knowledge CRUD API](0038-information-payload-knowledge-crud-api.md)
- [0039 - A generic `v_effective_stat` view, read by everything that shows stats](0039-generic-effective-stat-view.md)
- [0040 - Item-instance read visibility narrows to reachable owners](0040-item-instance-read-visibility.md)
- [0041 - Quantity and stacking, on `Containment`](0041-containment-quantity-and-stacking.md)
- [0042 - Concurrency token on reads](0042-concurrency-token-on-reads.md)
- [0043 - Item-instance slug](0043-item-instance-slug.md)
- [0044 - Loot assignment: split-with-owner, merge, and bulk-assign](0044-loot-assignment-split-merge-bulk-assign.md)
- [0045 - Read-only groups API](0045-read-only-groups-api.md)
- [0046 - GM reachability widens to the party's surroundings, not just their inventory](0046-gm-reachability-widens-to-surroundings.md)
- [0047 - Item catalog search and container-capability convention](0047-item-catalog-search-and-container-convention.md)
- [0048 - CORS configuration](0048-cors-configuration.md)
- [0049 - `mine` filter on `GET /tenants/{tenant_id}/characters`](0049-list-characters-mine-filter.md)
- [0050 - loot-bot: stack, account linking, and data isolation](0050-loot-bot-stack-linking-and-isolation.md) — originally numbered 0042 on `feat/loot-bot`, renumbered a second time on merge into `main`: `main` had independently claimed 0042-0049 for the apps/api additions above in the meantime. Picked the first number free of collision at merge time again, same precedent as the first renumbering.
- [0051 - loot-bot: `/give`, the first write command (loot-splitting)](0051-loot-bot-give-command.md)
- [0052 - loot-bot: loot drop, take, claim/unclaim, apply claims](0052-loot-bot-loot-drop-and-claims.md)
- [0053 - loot-bot: HTTP interactions transport and Cloud Run deployment](0053-loot-bot-http-interactions-and-cloud-run-deploy.md)
- [0054 - User identity: Authgear-synced email + local nickname](0054-user-identity-email-and-nickname.md) — renumbered from 0050 on merge into `main`, same precedent as loot-bot's own renumbering above: `main` had independently claimed 0050-0053 for loot-bot in the meantime.
- [0055 - Exact-match user lookup by email or nickname](0055-user-lookup-by-email-or-nickname.md)
- [0056 - Profile pictures for User, Tenant, and Campaign](0056-profile-pictures.md)
- [0057 - Platform operations: a platform-operator role, admin listing, and account suspension](0057-platform-operations.md)
- [0058 - Notifications: platform/tenant/campaign/character scope, in-app inbox](0058-notifications.md)
- [0059 - Group-scoped notifications](0059-group-scoped-notifications.md)
- [0060 - User profile expansion](0060-user-profile-expansion.md)
- [0061 - Notification sender read-receipts](0061-notification-sender-read-receipts.md)
- [0062 - Bulk invite to tenant](0062-bulk-invite-to-tenant.md)
- [0063 - Tenant activity log (first slice)](0063-tenant-activity-log.md)
- [0064 - Group write API: create/rename/delete, membership, bulk add, duplicate](0064-group-write-api.md)
- [0065 - Bulk item-instance container move](0065-bulk-item-instance-container-move.md)
- [0066 - `is_container` as a computed field, mirroring the tag](0066-is-container-computed-field.md)
- [0067 - `title` falls back to `name` when empty](0067-item-title-falls-back-to-name.md)
- [0068 - loot-bot: inventory hygiene, GM toolkit, claim tiers, per-channel preferences](0068-loot-bot-inventory-and-gm-toolkit.md) — originally numbered 0064 on `feat/loot-bot-inventory-and-gm-tools`, renumbered on merge into `main`: `main` had independently claimed 0064-0067 for the apps/api additions above in the meantime. Same renumbering precedent as ADR 0050/0054's own history.
- [0069 - apps/api additions requested by loot-bot: container flag, group-membership writes, bulk container move](0069-item-instance-container-flag-group-writes-bulk-container-move.md) — originally numbered 0065, renumbered alongside ADR 0068 above; superseded by ADR 0064/0065/0066 once those landed for real.
- [0070 - Planning: GitHub Milestones/Issues alongside RFC/ADR, early RFC/ADR merges, and a deferred roadmap](0070-planning-milestones-issues-and-a-deferred-roadmap.md) — numbered past 0068/0069, already claimed by a *different* branch's open, unmerged PR at the time this was written (that branch's own copies of 0068/0069 are now stale duplicates of the real ones above, and will need their own renumbering pass when it merges — a live fourth example of exactly the collision this ADR addresses); see its own Context section
- [0071 - account-hub: stack, auth, deploy, and e2e testing](0071-account-hub-stack-auth-deploy.md) — foundation decision for [RFC 0013](../rfcs/0013-account-hub-app.md): forks `apps/inventory-web`'s Astro/`@authgear/web`/Cloudflare Pages pattern verbatim, no component framework, and introduces Playwright as this repo's first browser e2e tool
- [0072 - Item catalog prototype-set editing](0072-item-catalog-prototype-set-editing.md)
- [0073 - Item prototype graph inspection and bulk editing](0073-item-prototype-graph-inspection-and-bulk-editing.md)
- [0074 - account-hub: campaign visibility on /characters, and shared user lookup](0074-account-hub-campaign-visibility-and-user-lookup.md) — first sub-slices of [RFC 0014](../rfcs/0014-account-hub-tenant-admin-and-roster-management.md); a reusable exact-match user-lookup module and picker widget, built once for the GM-assignment and player-invite sub-slices to share
- [0075 - Sync user email via Authgear's UserInfo endpoint, not the access token](0075-sync-email-via-userinfo-not-access-token.md) — numbered past 0074, already claimed by the in-flight `apps/account-hub` work ([issue #88](https://github.com/ramsesoriginal/lorenzo/issues/88)) at the time this was written; see [issue #89](https://github.com/ramsesoriginal/lorenzo/issues/89) for the bug this decides
- [0076 - account-hub: campaign creation, GM assign/revoke, player invite](0076-account-hub-tenant-admin-actions.md) — three [RFC 0014](../rfcs/0014-account-hub-tenant-admin-and-roster-management.md) sub-slices grouped into one ADR (shared home `/tenants`, shared ADR 0074 user picker); the GM-revoke list can only show raw `user_id`s, since `GmOut` has no display name and no reverse user lookup exists in this API — addendum adds editing an existing campaign, surfaced by manual testing
- [0077 - Unowned item-instances endpoint, grouped like owned-by](0077-unowned-item-instances-endpoint.md) — numbered past 0076, already claimed by the in-flight `apps/account-hub` work (open PR at the time this was written); see [issue #95](https://github.com/ramsesoriginal/lorenzo/issues/95)
- [0078 - Being listing and search endpoint](0078-being-listing-and-search-endpoint.md) — see [issue #94](https://github.com/ramsesoriginal/lorenzo/issues/94)
- [0079 - account-hub: character/being roster reuse across campaigns](0079-account-hub-roster-reuse-and-being-handoff.md) — last two [RFC 0014](../rfcs/0014-account-hub-tenant-admin-and-roster-management.md) sub-slices (player character reuse, GM being-to-player handoff), both built on the same `character_player` roster-link write; also upgrades `/beings`'s listing to ADR 0078's new endpoint, replacing an earlier client-side-filter workaround
- [0080 - account-hub: CSS conventions, shared DOM helpers, page decomposition](0080-account-hub-css-conventions-and-shared-dom-helpers.md) — engineering cleanup: defines four previously-classless markup rules in `components.css`, extracts `src/lib/dom.ts` to remove real duplication between `characters.astro`/`beings.astro`, and splits `tenants.astro` incrementally as [RFC 0017](../rfcs/0017-account-hub-roster-tenant-admin-and-notifications.md)'s admin features land
- [0081 - account-hub: campaign roster view, being search, self-service leave](0081-account-hub-campaign-roster-search-and-leave.md) — [RFC 0017](../rfcs/0017-account-hub-roster-tenant-admin-and-notifications.md) (a)/(b)/(c): a per-campaign roster view (and a "while here" real-name upgrade for the GM-revoke/being-handoff lists), a search input on `/beings`, and self-service "leave a campaign" on `/characters`
- [0082 - account-hub: tenant creation, tenant-level membership admin, bulk invite](0082-account-hub-tenant-creation-and-membership-admin.md) — [RFC 0017](../rfcs/0017-account-hub-roster-tenant-admin-and-notifications.md) (d)/(e): an always-shown create-tenant form (no client-visible signal for the gating role exists), and owner-gated tenant membership CRUD + bulk invite
- [0083 - account-hub: tenant activity log, notification sending, sent notifications](0083-account-hub-activity-log-and-notification-sending.md) — [RFC 0017](../rfcs/0017-account-hub-roster-tenant-admin-and-notifications.md) (f)/(g)/(h): a tenant activity log, a shared tenant/campaign-scope notification composer with an optional recipient, and a sent-notifications view grouped by batch with per-recipient read state
- [0084 - Activity log coverage policy and a member-removal notice](0084-activity-log-coverage-and-member-removal-notice.md) — states what ADR 0063's log is for and the rule for what it records (any actor, GMs included); pins the read gate as administrators-only (already true, with a role-added tripwire test); adds `detail` to `membership.deleted` plus a removal notification
- [0085 - Tenant data export: audit of existing reads, three small missing read endpoints, and a runbook](0085-tenant-data-export-audit-and-runbook.md) — accepted; finds stat group/definition listing and knowledge grants unreachable, flags a possible OWNER-vs-ORGA visibility hole, defers the runbook to the implementation slice
- [0086 - `GET /me/managed` and a `since` filter on `GET /me/notifications`](0086-managed-scope-aggregate-and-notifications-since-filter.md) — accepted; cross-tenant "what do I run" index and an inclusive `since` timestamp filter
- [0087 - account-hub: tenant/campaign pictures, an unread-notification badge, and a cross-tenant access overview](0087-account-hub-pictures-unread-badge-and-access-overview.md) — originally numbered 0085 on this branch, renumbered before merge: `feat/api-tenant-accountability-docs` ([PR #113](https://github.com/ramsesoriginal/lorenzo/pull/113)) independently claimed 0085 first (by commit timestamp) for its own tenant-data-export ADR, same renumbering precedent as ADR 0050/0054/0068/0069; [RFC 0019](../rfcs/0019-account-hub-pictures-overview-and-membership-safety.md) sub-slices 1-3, correcting two of that RFC's assumptions once checked against this app's real source (no shared nav exists - `index.astro` is the hub; `GET /me` alone can't name tenants/campaigns - `/tenants`' own `listMyTenants`/`listTenantCampaigns` aggregation is reused instead)
- [0088 - loot-bot: `/give` confirmation and last-used-character memory](0088-loot-bot-give-confirmation-and-last-used-character.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 3; `/give` asks with a one-click Give/Cancel first, its whole intent in the button `customId` (Cloud Run can route the click to another instance) and the buttons stripped by the click's own acknowledgement so a double-click cannot split a stack twice; the acted-on item's owner (if it is one of your characters) becomes your server-wide default, since no command takes an own-character option; fixes `getPreference`'s per-row fallback that let a container-only channel row hide it
- [0089 - loot-bot: `/help` grouped by task, with a first-run intro](0089-loot-bot-task-grouped-help.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 4; regroups the same 26 commands by goal in newcomer order (nothing renamed), adds one static intro sentence (not tailored to link state, since `/help` must not depend on the database) and audience notes on GM groups, a "Part of" footer on the detail view, and a test that checks every real command is grouped
- [0090 - loot-bot: `/sheet`](0090-loot-bot-sheet.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 5a; a private view of one character's resolved stats and the notes the caller is cleared to see, reusing `/item's renderer since the API returns the same shape; defaults to your current character, else your only one; visibility is the API's, never the bot's
- [0091 - inventory-web: a generated, typed API client](0091-inventory-web-typed-api-client.md) — adopts `apps/loot-bot`'s own `openapi-typescript`/`openapi-fetch` pattern (ADR 0050) in place of the hand-rolled `apiFetch<T>(path)` and hand-maintained `lib/types.ts`; written after [PR #140](https://github.com/ramsesoriginal/lorenzo/pull/140) implemented it rather than before, a process gap this ADR's own Context section names directly
- [0092 - Shareable, expiring campaign invite links](0092-campaign-invite-links.md) — accepts RFC 0023: hashed 256-bit tokens, required expiry, optional use cap, player-only, login to redeem, edge rate rule plus in-process backstop
- [0093 - loot-bot: `/drop` container autocomplete and slug surfacing](0093-loot-bot-drop-autocomplete-and-slug-surfacing.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 2; `/drop container:` suggests unowned (a GM's ownerless loot pile) plus own containers, since the owned-only source every other container option uses could never list it; slugs shown as autocomplete-name suffixes, inline code in `/inventory`, and a copyable fenced block in `/item`
- [0094 - loot-bot: `/container-new`](0094-loot-bot-container-new.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 5b; a named sack owned by your character plus a multi-select that moves your loose items in; a player can instantiate a prototype for their own character but cannot even list the catalog (every `/items` route needs a tenant `Membership`), so the sack prototype's id is found-or-created once by whoever first has catalog access and stored (`container_prototype`), with an ask-an-admin message until then; named `container-new` since the adapter has no subcommands; an abandoned picker leaving an empty sack is accepted
- [0095 - loot-bot: notification DMs, with a "couldn't DM you" fallback](0095-loot-bot-notification-dms.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 6; a Cloud Scheduler-triggered run DMs each linked user their own unread Lorenzo notifications, read as the recipient and never marked read (a bot-side ledger is the cursor); the route is off unless configured and only serves the scheduler's Google-signed OIDC token for one expected service account; only this tenant's or platform notifications created after a user was first seen, capped per run; Discord's "DMs closed" error queues the notification and a private "couldn't DM you" banner shows on the user's next slash command
- [0096 - Tenant OWNER joins ORGA in the information-visibility bypass](0096-owner-joins-orga-in-the-information-visibility-bypass.md) — amends ADR 0035/0028: an OWNER reads GM-only information like an ORGA (per-campaign opt-out unchanged), found by ADR 0085's export audit; originally numbered 0091, renumbered before merge: `main` independently claimed 0091 (inventory-web typed API client) while this was in review, so it moved past every number claimed on any branch at the time (highest then 0095). Same renumbering precedent as ADR 0050/0054
- [0097 - loot-bot: `/changes`, a bot-recorded view of what happened to your stuff](0097-loot-bot-changes.md) — [RFC 0021](../rfcs/0021-loot-bot-player-toolkit.md) slice 7; an event log (keyed by character, since the bot cannot look up who plays one) written after each give, bulk give, reassign, award, confiscation, drop take and honored claim, and read as "since you last looked"; GMs never named; every reply says it only covers changes made through the bot, until [RFC 0022](../rfcs/0022-player-facing-change-feed-api.md)'s API feed makes it complete
- [0098 - Branding CSS: apps/brand + packages/brand, same-origin default](0098-branding-css-app-and-package.md) — accepts [discussion #133](https://github.com/ramsesoriginal/lorenzo/discussions/133)'s third proposal: a hand-edited `apps/brand/tokens.css` (plain static site, no build step, previewable via `file://`) deployed to Cloudflare Pages, plus a thin `packages/brand` npm package copying that same file for other apps to bundle at build time; consuming apps default to a same-origin bundled copy and can opt into pointing at the deployed host instead for instant cross-app propagation, keeping self-hosting dependency-free by default
- [0099 - A player-facing change feed](0099-player-facing-change-feed.md) — accepts RFC 0022: a per-recipient `entity_change` table readable only by its recipient, rows for what a character owns or carries, actor shown only when it is another player, 90-day retention, `GET /me/changes`
- [0100 - LorenzoScript core: `packages/lorenzoscript` parser and renderer](0100-lorenzoscript-core-parser-and-renderer.md) — [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 1; a zero-dependency, source-only TypeScript package (`parse` → syntax tree → `render` → HTML) covering the core syntax, with URL/escaping safety enforced in the renderer alone; containers parsed by collect-strip-recurse, CommonMark's delimiter algorithm driven by a per-character table later stages extend; a 32-level nesting limit plus linear-time emphasis and lazy continuation, each forced by a hostile-input test; `SPEC.md`'s embedded examples are the test suite; CI's `discover` now finds `packages/*` too
- [0101 - Editable information: singleton types, ordering, and description edits](0101-editable-information-and-description-payloads.md) — accepts [RFC 0015](../rfcs/0015-information-metadata-shape.md) sub-slices 1 and 3 plus description edits from sub-slice 4: `information_type` plus a partial unique index, so only `description`/`main_picture` stay one per entity; `order` on `information` and `payload`; `PATCH`/`DELETE /information/{id}` and `PATCH /payloads/{id}` with `If-Match`; editing needs self-or-managed standing plus sight of the row (or authorship, via a new `information.created_by`), which closes a self-grant hole in the knower routes too; every description write goes through one function that RFC 0027 stage 7 will extend. RFC 0015 sub-slice 2 (`entity_slug`) is left to RFC 0027
- [0102 - LorenzoScript standard extensions](0102-lorenzoscript-standard-extensions.md) — [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 2; `~sub~`/`~~del~~`/`^sup^` as same-length delimiter rows; footnote and abbreviation definitions collected as document data and resolved at render; one `{#id .class}` token list for headings, code fences, inline elements, and `{{.class}}` blocks/spans; every heading gets a slug id, which `{{TOC}}` links to; GitHub tables with `align`; `$…$`/`$$…$$` as a documented TeX subset rendered to MathML Core, falling back to code outside it; numbered past 0101, claimed first by the description-editing work
- [0103 - Stats: tag endpoints, enum values, and mandatory groups](0103-stat-tags-enum-values-and-mandatory-groups.md) — accepts [RFC 0016](../rfcs/0016-stats-computed-values-and-crud-api.md) sub-slices 1 to 3: `PUT`/`PATCH`/`DELETE .../entities/{id}/tags/{stat_definition_id}` for true, explicit false, and inherit, with `PUT`/`PATCH` acquiring the stat's group (ADR 0037's named gap); an `enum` value type stored in `value_text` and checked against a tenant-scoped `stat_definition_enum_value` vocabulary; a display-only `stat_group.mandatory` flag
- [0104 - Computed stats: linear and comparison formulas](0104-computed-stats.md) — accepts [RFC 0016](../rfcs/0016-stats-computed-values-and-crud-api.md) sub-slices 4 and 5: `computed_stat` with `linear` (multiplier, offset, explicit rounding mode, fixing the D&D modifier's floor-vs-truncate bug) and `comparison` (bool, or two text/enum results) kinds; `v_effective_stat` lets a formula win at a hop like a stored value, and a Python pass evaluates it against the entity being read; tenant-admin authoring with a tenant-wide definition-level cycle check, a dry-run preview, and a "what depends on this stat" lookup
- [0105 - LorenzoScript entity references, dates, and the resolver](0105-lorenzoscript-entity-references-and-resolver.md) — [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 3; entity links and images are `link`/`image` nodes with a `ref`, from `[text](hint/slug)` (slug exact) or `[[Name]]` (slugified); a synchronous resolver renders them, and unresolved ones are indistinguishable from plain text so nothing hints at hidden entities; `{{date YYYY-MM-DD}}` in the viewer's locale and reserved `{{cal …}}` as the first inline directives; `references()` returns everything to batch-resolve, de-duplicated in document order, and SPEC.md's examples can assert it; numbered past 0101–0104, claimed by other open PRs
- [0106 - LorenzoScript editor: `packages/lorenzoscript-editor` and a static demo](0106-lorenzoscript-editor-and-demo.md) — [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 4; every edit a pure `(text, selection)` function behind a configurable toolbar, applied through `execCommand('insertText')` so native undo works; enhances an existing `<textarea>` in place; Enter continues lists, Tab indents, and Escape-then-Tab leaves; split or tabbed live preview with an injectable, possibly async `render`; editor and rendered-text styles go in `apps/brand` (ADR 0098), not the package; a demo opening from `file://` via a gitignored esbuild IIFE bundle
- [0107 - Entity slugs, and resolving them in one request](0107-entity-slugs-and-batch-resolve.md) — accepts [RFC 0015](../rfcs/0015-information-metadata-shape.md) decision 5 as [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 5: an `entity_slug` table (one slug per entity, unique per tenant, row-level secured) replacing `item_instance.slug`; RFC 0027's slug grammar checked on every write, item-instance creation included (a deliberately accepted breaking change); `GET .../entities/by-slug/{slug}`, `PUT`/`DELETE .../entities/{id}/slug`, and a batch `GET .../entities/resolve?slug=` returning each existing slug's entity id, name, and kinds; resolving shares `GET /entities/{id}`'s gate, so a slug resolves for exactly the readers who could open the entity
- [0108 - LorenzoScript in inventory-web: rendered descriptions and a description editor](0108-lorenzoscript-in-inventory-web.md) — [RFC 0027](../rfcs/0027-lorenzoscript.md) stage 6; the item page and board panel render descriptions through one shared helper (batched slug resolution cached per page, main pictures fetched with the viewer's token as `blob:` URLs, `/me`'s locales); links open the item page or the board by view hint, then by the entity's kinds; GMs edit on the item page (`PATCH` with `If-Match`, or a new description that's public by default); `etag_for` now writes the JSON `updated_at` text, so ADR 0101's `If-Match` rule works
