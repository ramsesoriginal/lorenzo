# Reference

- **API**: `apps/api`'s live OpenAPI/Swagger UI (`/docs`, `/healthz`, `/readyz`, `/metrics` documented there) is the reference while running it locally — see [docs/guides/getting-started.md](../guides/getting-started.md). A generated static export will land here once the API's surface is stable enough to be worth freezing (no domain endpoints yet).
- **Config**: see [.env.example](../../.env.example) at the repo root.
