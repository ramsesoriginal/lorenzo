# Reference

- **API**: once the backend API exists, its live OpenAPI/Swagger UI will be the reference while running locally. A generated static export will land here once the API's surface is stable enough to be worth freezing.
- **Config**: see [.env.example](../../.env.example) at the repo root.
