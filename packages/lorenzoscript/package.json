{
  "name": "@lorenzo/lorenzoscript",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "LorenzoScript, Lorenzo's Markdown dialect: a zero-dependency parser and HTML renderer. See RFC 0027 and ADR 0100.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check . && tsc --noEmit",
    "format": "biome check --write .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11"
  }
}
